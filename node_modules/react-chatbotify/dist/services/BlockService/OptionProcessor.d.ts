/// <reference types="react" />
import { Block } from "../../types/Block";
/**
 * Handles processing of options in current block.
 *
 * @param block current block being processed
 * @param path path associated with the current block
 * @param injectMessage utility function for injecting a message into the messages array
 * @param handleActionInput handles action input from user
 */
export declare const processOptions: (block: Block, path: string, injectMessage: (content: string | JSX.Element, sender?: string) => void, handleActionInput: (path: string, userInput: string, sendUserInput: boolean) => Promise<void>) => void;
